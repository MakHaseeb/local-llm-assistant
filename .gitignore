# Virtual environment (each person creates their own)
.venv/

# Python cache files
__pycache__/
*.pyc

# Secrets (not needed for a local model, but a good habit)
.env

# macOS folder metadata
.DS_Store
